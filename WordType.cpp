//Represents the type of tokens (word,punct,etc.)

//An enum is a special type that represents a group of constants (unchangeable values).
//By default, the first item (WORD) has the value 0, the second (PUNCT) has the value 1, etc.
enum WordType {
	WORD,
	PUNCT,
	NUM,
	ALPHANUM ,
	ENDL
};